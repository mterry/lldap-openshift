kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: $LLDAP_APP_NAME
  namespace: $LLDAP_NAMESPACE
  labels:
    app: $LLDAP_APP_NAME
spec:
  to:
    kind: Service
    name: $LLDAP_APP_NAME-service
  tls:
    null
  port:
    targetPort: '$LLDAP_ADMIN_PORT'
  alternateBackends: []
        
